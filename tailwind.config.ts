import type { Config } from 'tailwindcss'

const config: Config = {
    theme: {
        extend: {
            fontFamily: {
                propo: ['Propo'],
                lxgw: ['LXGW'],
            },
        },
    },
    plugins: [],
}

export default config